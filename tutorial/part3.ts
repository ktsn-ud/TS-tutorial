// tutorial/part3.ts

// TODO: `name`(文字列)、`age`(数値)、`department`(文字列) の3つのプロパティを持つ `userProfile` という名前のオブジェクトを作成してください。
export const userProfile = {
  // ここにプロパティを追加
};

// TODO: あなたの好きな食べ物を3つ以上含む、`favoriteFoods` という名前の配列を作成してください。
export const favoriteFoods = [];
