// tutorial/part5.ts

// TODO: `name` (文字列) と `age` (数値) を引数として受け取り、
// 「My name is [name], and I am [age] years old.」という文字列を返す `getUserInfo` という関数を、TypeScriptの型を使って作成してください。
export function getUserInfo(name: string, age: number): string {
  return ""; // この行を修正
}
