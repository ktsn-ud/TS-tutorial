// tutorial/part4.ts

const numbers = [1, 2, 3, 4, 5, 6];

// TODO: `numbers` 配列の各要素を2倍にした新しい配列を返す `doubleNumbers` という関数を `.map()` を使って作成してください。
export function doubleNumbers() {
  // return numbers.map(...)
  return [];
}

// TODO: `numbers` 配列の中から偶数だけを抽出した新しい配列を返す `getEvenNumbers` という関数を `.filter()` を使って作成してください。
export function getEvenNumbers() {
  // return numbers.filter(...)
  return [];
}
