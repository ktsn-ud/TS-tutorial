// tutorial/part2.ts

// TODO: 2つの数値を受け取り、その合計を返す `add` という名前の関数を作成してください。
// 例: add(1, 2) は 3 を返す
export function add(a: number, b: number) {
  return 0; // この行を修正
}

// TODO: 文字列を受け取り、「Hello, [受け取った文字列]!」という文字列を返す `greet` という名前のアロー関数を作成してください。
// 例: greet("Alice") は "Hello, Alice!" を返す
export const greet = (name: string) => {
  return ""; // この行を修正
};
